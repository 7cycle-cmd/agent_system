# Copy to .env and fill in. Do not commit .env.

# OpenClaw Windows Companion → Permissions → Local MCP Server (ON)
# Bridge must run on Windows (Companion listens on Windows loopback).
OPENCLAW_MCP_URL=http://127.0.0.1:PORT/mcp
OPENCLAW_MCP_TOKEN=aD3PWAvGJ7wtSXlfG4qIUdb3C_K80sMU6JpdnjMeB0I
OPENCLAW_MCP_TIMEOUT=60

# Ollama vision (WSL OpenClawGateway often on 18803; ensure Windows can reach it)
OLLAMA_BASE_URL=http://127.0.0.1:18803
OLLAMA_VISION_MODEL=qwen2.5vl:7b
OLLAMA_TIMEOUT=180
